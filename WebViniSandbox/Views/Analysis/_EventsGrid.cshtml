@using MvcContrib.UI.Grid;
@using MvcContrib.Pagination;
@using MvcContrib.Shp.UI.Themed.Pager;

@model IPagination<ViniSandbox.Models.computer_event>

<div class="mc-grid ui-widget ui-widget-content ui-corner-all">
    <div class="mc-grid-header ui-widget-header ui-corner-top">Eventos</div>        
    @Html.MvcContrib().Grid(Model).WithModel(new WebViniSandbox.Models.GridModel.ComputerEventGridModel()).Sort(ViewData["sort"] as GridSortOptions).Attributes(id => "grid", style => "width: 100%;")
    @Html.MvcContrib().PagerThemed(Model)
</div>